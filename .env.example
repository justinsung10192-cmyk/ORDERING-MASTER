# Google Apps Script Web App 端點（訂餐資料實際存放處）。
# 在 Vercel 專案的 Settings → Environment Variables 中設定同名變數，
# 部署時 Vercel 會以環境變數提供給 api/gas.js 使用。
GAS_WEB_APP_URL=https://script.google.com/macros/s/你的部署ID/exec
