# Google Apps Script 部署步驟

本專案的前端為靜態單頁應用程式；`Code.gs` 必須貼入與資料試算表綁定的 Google Apps Script 專案並部署為 Web App，才能啟用帳號、訂餐、錢包、QR 驗證與郵件排程。

| 步驟 | 操作 |
| --- | --- |
| 1 | 建立一份新的 Google 試算表，從「擴充功能 → Apps Script」開啟綁定式指令碼專案。 |
| 2 | 從本次交付的完整 `class-lunch-Code.gs.zip` 解壓取得 `Code.gs`；在 Apps Script 編輯器中**全選並覆蓋**原本檔案的全部內容後儲存。請勿只複製聊天中的局部程式碼，也不要與舊版片段混用。 |
| 3 | 在 Apps Script 左側「專案設定 → 指令碼屬性」設定 `FRONTEND_URL` 為你的正式網站網址（部署 Vercel 後即為 Vercel 網址；目前為 `https://classlunch-czdbzqtt.manus.space`）、`EMAIL_DOMAIN`（目前可留白）、至少 8 碼的 `ADMIN_AUTH_CODE`，以及僅由系統開發者保管的高強度 `DEVELOPER_MASTER_KEY`。第一次執行 `setupSystem()` 會自動寫入 `SPREADSHEET_ID` 與安全雜湊 Salt；`DEVELOPER_MASTER_KEY` 不可放在前端或交給班級管理者。 |
| 4 | 在函式選單選擇 `setupSystem` 後按「執行」。首次授權需允許試算表、寄信與觸發器權限；它會建立訂餐資料表、`DeveloperAccounts` 開發者帳號表、標題列與每小時提醒觸發器，並安全補上 `Users` 的信箱驗證欄位。 |
| 5 | 首次部署請選擇「部署 → 新增部署作業 → 網頁應用程式」。執行身分選擇自己，存取權應選擇可讓本網站伺服器存取的公開範圍（通常為「所有人」）；完成後保留 Web App URL。日後更新請至「部署 → 管理部署作業 → 編輯」，建立**新版本**後按「部署」，以維持同一個 `/exec` URL。 |
| 6 | 對 Vercel 部署，本專案以 `api/gas.js` 提供 `/api/gas` 同網域代理；請在 Vercel 專案的 Environment Variables 設定 `GAS_WEB_APP_URL` 為你的 GAS Web App URL，無須在前端直接填寫 GAS 網址。若換用新的 GAS 部署，更新該環境變數後重新部署即可。 |
| 7 | 一般班級管理者先使用開發者核發的 `ADM-...` 班級管理者代碼註冊學生帳號，完成 Email 驗證後即成為該班 Admin。若是舊資料或既有帳號，仍可依「設定」中的管理員權限流程使用 `ADMIN_AUTH_CODE` 升級。之後即可管理店家、餐點、選項、訂餐場次、學生帳號與每場次訂購總表。 |

> **提醒：** 每次修改 Apps Script 後，須在「部署 → 管理部署作業」建立新版本並更新既有 Web App 部署。請將試算表與 Apps Script 的編輯權限限制於受信任管理者。Vercel 的 `api/gas.js` 同網域代理可避免瀏覽器直接呼叫 GAS 時發生跨網域限制，且已設定不快取動態帳號、訂餐及錢包回應。

## 同日多場次與場次管理

每一個訂餐場次都有獨立的 `SessionID`，因此可在同一天建立不同店家或不同用途的場次，例如「主食店」與「飲料店」。學生端會以「日期、店家、截止時間」分開顯示每個場次；同一名學生可以對兩個不同的 `SessionID` 分別下單，但無法重複送出同一場次的訂單。

管理員可在「管理 → 場次」中修改某一場次的截止時間、將其提前結束，或刪除尚未產生訂單的場次。刪除前系統會先檢查是否已經有訂單，以避免遺失既有訂餐紀錄。每次建立新場次時，系統會嘗試通知所有未停用、且具有有效電子郵件地址的使用者；畫面顯示的是 Apps Script 實際回傳的成功通知數，並不表示未部署時已寄出郵件。

## 場次總表、訂單異動與帳號刪除

在「管理 → 統計」選定日期後，系統會先顯示全日統計，接著依每個 `SessionID` 分成獨立的「各場次訂購總表」。每張總表包含店家、日期、截止時間、訂單筆數、總份數、未結清人數、已取餐份數、總應收金額，以及依餐點與客製選項合併的備餐數量，因此同日飲料與主食不會混在同一張訂購表中。

學生在「訂餐」頁的已完成訂單，若尚未到截止時間、尚未取餐、且尚未現金結清，可點選「修改訂單」調整餐點、數量、客製選項與備註，或選擇「取消訂單」。系統重新以更新後總額核對儲值餘額與未繳金額；取消以錢包支付的訂單時會退回已扣的部分。截止、完成取餐或現金結清後，學生端會鎖定自行異動。

管理員可在「管理 → 帳號」停用、恢復或刪除一般學生帳號。刪除會移除個資、登入憑證與未使用的驗證／重設資料；為維持對帳，既有訂單和交易紀錄不會刪除，統計中將以「已刪除帳號」呈現。系統不允許管理員刪除目前登入者或其他管理員帳號，避免意外失去管理權限。

所有會讀取或改變資料的送出型操作，前端會在請求完成前鎖定整個操作介面，避免網路延遲造成重複送單、重複儲值或同時執行兩種管理操作。

## 既有 Web App 回傳 `respond is not defined` 的修復

若目前 `/exec` URL 顯示 HTML 錯誤頁並提及 `ReferenceError: respond is not defined`，代表線上的 Apps Script 版本不是本專案完整的 `Code.gs`。請依上表完成「完整覆蓋 → 儲存 → 執行 `setupSystem` → 更新既有 Web App 部署」四個步驟。完成部署後，重新開啟該 `/exec` URL，應回傳 JSON，而非 Apps Script 的 HTML 錯誤頁。只有在此後，註冊驗證信與新場次通知信才會由實際 GAS 環境寄送。

## 資料欄位處理

`Orders` 依題目指定只有一個 `Options` 欄位，因此程式以 JSON 儲存每個所選餐點、數量、單價與加選項；這樣可支援複選餐點與單一餐點多份數，且不改變指定的九張工作表及其欄位。混合支付模式在餘額不足時會讓整筆訂單先成為 `UnpaidCash`；後續儲值會依建立時間優先抵扣欠款，餘額不足時維持為 0 元並以 `PartiallyPaid` 保留尚未抵完的金額。

## 郵件與觸發器

`sendCutoffReminders()` 會由 `setupSystem()` 建立的每小時觸發器執行。當未截止的場次距截止時間小於 12 小時，尚未下單且未被停用的學生會收到一次提醒。系統以指令碼屬性避免同一學生對同一場次重複寄信。Gmail 每日寄送額度依 Google 帳戶類型而定，班級規模較大時請留意 Apps Script 的配額。

## 註冊驗證信未送達時的處理

先確認 Apps Script 已貼上最新的 `Code.gs`、**儲存**，並將既有 Web App 更新為最新版本。接著在 Apps Script 編輯器手動執行一次 `setupSystem()`，並同意 Gmail 權限。完成後以管理員登入系統，在「管理 → 系統」按下「檢查 Gmail 授權與寄送額度」；此操作不會寄出測試信。如果顯示 Gmail 未授權，請回 Apps Script 重新執行 `setupSystem()` 或任一需要寄信的函式並完成授權；如果剩餘額度為 0，須等待帳戶額度重置後再按學生端的「重新寄送驗證碼」。學生也應檢查校務信箱的垃圾郵件或校內信件隔離區。

## 多班級全新初始化（破壞性操作）

本版本改為以班級隔離資料。因本次確認不保留既有資料，請在完成完整 `Code.gs` 覆蓋並儲存後，於綁定目標試算表的 Apps Script 編輯器手動執行 `setupSystemFresh('DELETE_ALL_DATA')`。此操作會清除 Users、Classes、DeveloperCodes、DeveloperAccounts、InviteCodes、Stores、MenuItems、ItemOptions、Sessions、Orders、Transactions、Verification 與 ResetTokens 的資料列，只保留新版表頭；請確認目前試算表不再需要任何舊資料後才執行。

初始化完成後，開啟網站登入頁底部的「開發者入口」，選擇「註冊開發者帳號」，輸入 `DEVELOPER_MASTER_KEY`、開發者帳號、Email 與至少 8 碼密碼。正確金鑰會在註冊時立即開通帳號；之後登入「開發者工作台」，即可在「管理者代碼」分頁輸入班級名稱並核發一次性 `ADM-...` 代碼；原始代碼只在核發完成的確認視窗顯示一次，請私下交給該班第一位管理者。管理者以此代碼註冊並完成一般 Email 驗證後，系統會建立自己的班級與管理者身分；管理者登入後可於設定頁產生、查看及停用一般使用者邀請碼。

一般使用者註冊時必須填寫學號、密碼、密碼確認、真實姓名、一般電子郵件、座號與邀請碼。學號是登入帳號，不會再組合成任何學校 Email；Email 僅用於驗證、密碼重設與班級通知。邀請碼和班級管理者代碼只能擇一，未完成 Email 驗證的帳號不可登入。

## 開發者代碼撤銷安全規則

開發者工作台可直接撤銷尚未使用的管理者代碼；撤銷後不能再註冊班級管理者，已使用的代碼不可恢復或再次啟用。開發者帳號目前採「正確金鑰即時開通」模型，沒有另外的待開通狀態；金鑰錯誤時不會建立帳號。開發者 API 會以獨立的 Developer session 保護，未登入、session 失效、停用帳號或非開發者 token 都會拒絕。舊版 Apps Script 函式 `developerRevokeClassAdminCode({developerKey, codeId})` 仍可作為維運後門，但不應在前端呼叫；`DEVELOPER_MASTER_KEY` 只應保存於指令碼屬性。

## 設定責任分工

| 設定內容 | 管理角色 | 設定範圍 | 操作位置 |
|---|---|---|---|
| 班級名稱與班級狀態 | 開發者／系統維運 | 單一班級 | 開發者工作台或維運流程 |
| 班級校務 Email 後綴 | 該班班級管理者 | 單一 ClassID | 管理介面 → 管理 → 設定 → 本班設定 |
| 班級邀請碼 | 該班班級管理者 | 單一 ClassID | 管理介面 → 管理 → 設定 → 班級邀請碼 |
| 管理員升級授權碼 | 開發者 | 全系統 | 開發者工作台 → 系統設定 |
| DEVELOPER_MASTER_KEY、SPREADSHEET_ID、PASSWORD_SALT | 開發者／系統維運 | 全系統 | Apps Script 指令碼屬性 |
| Gmail 授權與寄送額度 | Apps Script 執行身分 | 全系統服務 | Google 帳戶授權與 Gmail 配額 |

班級 Email 後綴現在儲存在 `Classes.EmailDomain`，每個 `ClassID` 各自獨立。班級管理者在自己的設定頁修改時，不會覆蓋其他班級；截止提醒與新場次通知也只會寄給相同 `ClassID` 的使用者。學生註冊仍須輸入完整的一般 Email；註冊驗證信與密碼重設信會直接寄到該完整 Email，不會自動拼接或替換。`EmailDomain` 目前是班級提示與設定資料，不會限制學生只能使用該後綴。

管理者設定頁不再修改全域 `ADMIN_AUTH_CODE`。如需建立或更換管理員升級授權碼，請由開發者登入工作台的「系統設定」分頁處理；`DEVELOPER_MASTER_KEY` 不提供網頁修改，仍只應保存於 Apps Script 指令碼屬性。
